# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  test_ml:
    if:
      - {{ source.diff.files | map(attr='new_file') | match(term='/dev/null') | some }}
    run:
      - action: add-comment@v1
        args:
          comment: |
            deleted file
