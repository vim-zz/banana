# -*- mode: yaml -*-


manifest:
  version: 1.0

automations:
  jira_ticket:
    if:
      - {{ pr.description | includes(regex=r/^- \[ \]/) | some }}
    run:
      - action: add-label@v1
        args:
          label: "incomplete-step"
          color: 'F6443B'
