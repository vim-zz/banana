# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  bump_check:
    if:
      - true
    run:
      - action: add-comment@v1
        args:
          comment: |
            BUMP? {{ pr.description | frogbot_bump }}
            SEMVER? {{ pr.description | frogbot_bump | semver }}
