# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
   approve_non_blacklist:
    if:
      - {{ files | match(regex=blacklist) | nope }}
    run: 
      - action: add-label@v1
        args:
          label: 'non_blacklist'
          color: '#DF9C04'

blacklist: r/^(ui|src)\//
