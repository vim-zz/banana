# -*- mode: yaml -*-
manifest:
  version: 1.0

automations:
  add_team_as_reviewer_1:
    if:
       - true
    run:
      - action: add-comment@v1
        args:
          comments: |
            pr.author: {{ pr.author | includes(list=['vim-zz', 'thing']) }}


automations:
  add_team_as_reviewer_2:
    if:
       - true
    run:
      - action: add-comment@v1
        args:
          comments: |
            pr.author: {{ pr.author | includes(list=['vim-zz', 'thing']) }}
