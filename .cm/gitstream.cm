# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  contribution:
    if:
      - {{ true }}
    run:
      - action: add-comment@v1
        args:
          comment: |
            vim-zz contributed {{ repo.contributors | selectattr('vim-zz') }} commits to this repo