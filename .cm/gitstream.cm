checks:
  change:
    is:
     reformatting: {{ source.diff.files | allFormattingChange }}

automations:
  allow_reformatting:
    if:
      - {{ checks.change.in.reformatting }}
    run:
      - action: add-labels@v1
        args:
          labels: [fmt-only]
      - action: approve@v1