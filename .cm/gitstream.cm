# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  double_review:
    if:
      - {{ (files | allDocs) and (files | match(regex=r/requirements\/dev\.txt$/) | nope ) }}
    run:
      - action: add-label@v1
        args:
          label: 'docs'