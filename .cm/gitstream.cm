# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  contribution:
    if:
      - {{ source.diff.files | reject(attr='new_file', regex='package-lock\\.json$') | isFormattingChange }}
    run:
      - action: add-comment@v1
        args:
          comment: |
            formatting change (ignore lock)