# -*- mode: yaml -*-

manifest:
  version: 1.0

automations:
  check_lgtm:
    if:
      - {{ pr.general_comments | filter(attr='content', regex=r/^(l|L)(g|G)(t|T)(m|M)$/) | some }}
    run:
      - action: add-label@v1
        args:
          label: LGTM