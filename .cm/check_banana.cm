
automations:
  banana_check:
    if:
      - true
    run:
      - action: add-github-check@v1
        args:
          check_name: "Banana in title"
          conclusion: {{ "success" if (pr.title | includes(regex=r/banana/)) else "failure" }}
