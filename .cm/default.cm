manifest:
  version: 1.0

automations:
  linearb_hcl_review:
    on:
      - pr_created
      - commit
    if:
      - {{ (source.diff.files | extractTerraformChanges) == 'ro' }}
    run:
      - action: add-comment@v1
        args:
          comment: its an RO change
